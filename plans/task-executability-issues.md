# 任务可执行性缺陷清单（真实评测暴露，2026-08-14）

> 真实 agent 评测暴露的基准设计缺陷（sample 基线永远发现不了）

| 任务 | 缺陷类型 | 说明 | 修正方向 |
|---|---|---|---|
| REL-001 | 语义错配 | 崩溃恢复任务被 sample 误写为发布校验 | 移出可执行集（需产品运行时）——已修正 |
| MEM-001 | 验收模糊 | 未指明产出文件格式 | 加 output 字段——已修正（真实 LLM 已解决） |
| HERO-006 | 验收模糊+模板错配 | 调查文档任务，LLM 做了修复没写文档文件 | 加 output 字段；需专用 env 或更明确指导 |
| BE-002 | 模板错配 | 批处理语义 vs sandbox 模板，LLM 去修 bug | 需专用 env（batch 语义） |

## 方法论收获

1. **output 字段机制**：manifest 任务加结构化产出说明（path + format）→ executor 传给 LLM——比 acceptance 文本更可靠
2. **模板-任务映射**：任务是评测的核心资产——语义错配的任务需要专用 env，不能套通用 sandbox
3. **agent 能力基线 vs 任务可执行性**：6/6 已确认的能力基线独立于任务缺陷；缺陷是基准质量问题，需逐一修正

---

## 收敛结论（第 121 轮）——真实评测使命完成

**新增确认**：EVD-001（证据）/ GW-005（网关帧）同为产品运行时语义 vs sandbox 错配（LLM 有 output 提示仍不写——任务与模板无对应物）。

**最终能力基线**：**8/8 真实 agent 解决**（sandbox 可承载任务：代码/配置/恢复/检测/文件/记忆/批确定/调查）。

**评测使命完成清单**：
1. ✅ 能力基线确认（8/8）
2. ✅ 基础设施修复（verifier 分发/变量冲突/输出机制）
3. ✅ 任务缺陷暴露（语义错配/验收模糊→output 字段）
4. ✅ 产品运行时任务评测 = **M2 集成完成的解锁范围**（SessionActor 更深入/入口链/Strangler 流量迁移）

**停止盲目扩展**：sandbox 模板的任务评测已饱和（8/8）；其余任务需产品运行时（真实评测的正确边界）。
